/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media.immo-facile.com',
            "th.bing.com"
        ],
    },
};

export default nextConfig;
